# Maison UI validation — 7 September 2026

The UI keeps `companion-control` unchanged. The product owner selected a light-only
appearance: warm neutral surfaces, color carried by companion shapes and faces.

Implemented: compact companion navigation and account menu, a quiet chat with draft-only
starting suggestions, progressive settings (personality, applications, automations, team),
collapsed routine/trigger creation, contextual automation actions, and full-screen mobile
settings. Identity saves explicitly. Activity history and the computer remain reachable
from settings; active work still exposes Activity directly in the chat header.

Validation:

- Full isolated verification with PostgreSQL 18 passed: `8808e56c97e0`.
- After focus-return and mobile touch-target refinements, all 44 frontend behavior tests
  and the production TypeScript/Vite build passed again.
- Manual authenticated local browser: desktop chat/settings; 390 × 844 mobile settings,
  routine creation form, account menu, connections and account pages. No horizontal overflow
  on the checked mobile pages. Escape closes settings and restores the opening button's focus.
- Emulated dark OS preference still produces a light color scheme. Browser reported no page errors.
- Browser checks did not submit tasks, create automations, connect providers or open computers.
  No live Box was started. Isolated verification resources were cleaned up; the user-requested
  Herdr development stack remains available at http://127.0.0.1:4410.

This pass does not introduce autosave, new MCP operations, or change runtime behavior.
Screenshots remain private local artifacts because the inspected development account contains
real companion names and conversations.
