# Maison V2 validation — 7 September 2026

Delivered navigation: Discussion, Automations and Team are direct companion views. URLs
restore the selected section, browser Back returns to the prior section, and the mounted
chat preserves its unsent draft. Settings retains direct personality, applications,
computer and client-delivery access. Creation exposes appearance and computer choices
progressively. Provider marks use local assets from the original Companion catalog.

Team composition uses persisted template permissions, separate from specialist conversations.
The three-step wizard supports an existing or new coordinator, existing specialist profiles,
and review. New profiles can be created from Team. Adding a specialist does not spawn it.
Existing active permission limits are preserved. Partial saves retain the created coordinator;
ambiguous profile creation requires checking saved profiles before another create attempt.
Manual specialist-task retries retain a command id. Permanent-companion delegation and
machine-adoption UI remain outside this pass; the existing MCP capabilities are unchanged.

Verification:

- Full isolated PostgreSQL 18 / agent / server / frontend / recovery suite passed:
  `8e62f1b109aa`. Includes the new API test proving `prepare:false`, no run, durable retry,
  and conflict on changing the preparation preference under the same creation id.
- After final frontend recovery and loading refinements: **55 frontend tests passed**;
  TypeScript and production Vite build passed.
- Team and wizard load in separate chunks. Initial JS: 497.90 kB, 150.24 kB gzip;
  Team chunk 12.03 kB, wizard 9.32 kB. These are bundle sizes, not measured startup latency.
- Manual browser checks at 390, 768 and 1440 px: team, navigation, automation views,
  creation steps and local provider marks. No horizontal overflow on inspected layouts.
  Emulated dark system preference remains light; reduced-motion mode and browser errors checked.
- An authenticated browser created a disposable coordinator with an existing specialist.
  Direct database verification: provider Box, prepare_requested=false, box_id=NULL,
  zero runs, one permission. No Box was created. Browser Back preserved an unsent chat draft.
- The disposable coordinator and its sole permission were deleted after verifying no Box,
  preparation or runs. Existing profiles and other companions were preserved.
- Verification containers were cleaned up. The user-requested local development server was
  restarted after checking zero active runs and remains at http://127.0.0.1:4410.

Screenshots are private local artifacts because the inspected account contains real names
and conversations. This pass does not claim a production deployment or full user research.
